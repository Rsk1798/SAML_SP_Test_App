@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}


@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error</h1>
<h2 class="text-danger">An error occurred while processing your request.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<div class="card mt-4">
    <div class="card-body">
        <h5 class="card-title">Common SAML Issues</h5>
        <ul>
            <li>
                <strong>Certificate Issues:</strong> Ensure that the certificates are valid and properly configured
                in both the Service Provider and Identity Provider.
            </li>
            <li>
                <strong>Entity ID Mismatch:</strong> Verify that the Entity ID configured in the Identity Provider
                matches the Entity ID in this application's configuration.
            </li>
            <li>
                <strong>Endpoint URLs:</strong> Check that the Assertion Consumer Service URL and Single Logout
                Service URL are correctly configured in the Identity Provider.
            </li>
            <li>
                <strong>Authentication Context:</strong> Ensure that the requested Authentication Context is
                supported by the Identity Provider.
            </li>
            <li>
                <strong>Clock Skew:</strong> Verify that the clocks on the Service Provider and Identity Provider
                servers are synchronized.
            </li>
        </ul>
    </div>
</div>

<div class="mt-4">
    <a asp-controller="Home" asp-action="Index" class="btn btn-primary">Return to Home</a>
</div>